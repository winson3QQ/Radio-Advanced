# 瀑布圖參數 Demo 教學腳本（RTL-SDR + SDR++）

> 用 1~2 台手持無線電，在 SDR++ 瀑布圖上把每個參數「指給學員看」。
> 每個 demo 都是：**先看現象 → 一句話結論 → 場景怎麼調**。深理論略過。

---

## 0. 現場固定設定（開課前先設好）

| 項目 | 值 | 備註 |
|---|---|---|
| SDR 天線 | **拔掉或只留一小截** | 手持機在同室訊號極強，接天線會過載出一堆假訊號 |
| Source Gain | **0 ~ 15 dB** | 手持機很近，低增益即可；太高會過載 |
| IQ Correction | **勾** | 壓低中央 DC 假訊號 |
| Samplerate | 2.4 MHz | |
| RTL AGC / Tuner AGC / Bias-T | **全關** | 關 AGC 顏色/強度才可比較 |
| Radio 模式 | **NFM** | 手持機語音；WFM 是廣播用 |
| Bandwidth | 16000 | 收得夠寬，不切掉訊號 |
| Snap Interval | 12500 | Demo 步進用 |
| Squelch | Off | 上課要一直看得到 |
| 中心頻率 | 比無線電頻率**高/低約 100kHz** | 讓真載波離開中央 DC，例如機器 144.900、SDR 設 145.000 |
| 縮放 | 滾輪放大到 **±50 kHz** | 才看得清寬度與位置 |

**看圖三個基本功（開場先講）**
1. **左右位置 = 頻率**
2. **寬度 = 佔用頻寬**
3. **亮度/顏色 = 相對強度**（不是絕對功率；顏色由你設的色階決定）

**分辨真假訊號的萬用招：按 PTT ↔ 放開。** 跟著 PTT 亮滅的才是真電台；不理你的是假訊號（過載 spur、DC 尖峰、環境台）。

---

## Demo 0 ── 發射端校準：MIC GAIN 與調變位準
**機器選單：MIC GAIN　｜　開始任何 demo 之前先做一次**

> 上面第 0 節校的是**接收端**（SDR 增益、色階）。這一節校**發射端**。
> 兩邊都固定住，後面的 demo 才互相可比。

**為什麼一定要做**：FM 的佔用頻寬由**實際頻偏**決定，而實際頻偏由**音訊大小**決定。
講話音量每次都不一樣 → 訊號寬度就跟著跳 → **Demo 3 的 W/N 差異會被你自己的嗓音蓋掉**。
（實測過：用嘴吹氣做四組 W/N 對照，寬度差異完全被吹氣力道主宰，四組拼不出結論。）

**做法**（用 `TestTone/` 的固定音源，不要靠嘴巴）

1. 手機播 `1kHz_staircase.wav`，喇叭離麥克風約 5~10 cm，**手機音量設定一次就鎖死**。
2. 按住 PTT 讓階梯跑完（約 30 秒，先確認 TOT 夠長）。
3. 盯著訊號寬度：一階一階變寬 → 到某一階**不再變寬** ← **那就是頻偏限幅器的天花板**。
4. **記錄下來**：手機音量、距離、MIC GAIN 值、平台落在第幾階。之後所有 demo 都用這一組。

**順便看到過偏移**：平台之後繼續加音量，訊號兩側會長出不規則毛邊，潑到鄰頻 —— 那就是 splatter。

- **一句話**：W/N 設的是頻偏**天花板**，你的音量決定實際走到哪裡。不夠大聲用不滿頻寬，太大聲就潑到鄰台。
- **關鍵落差**：**過偏移的人自己完全沒感覺**。你的機器不會告訴你「剛才潑到隔壁頻道了」，只有鄰頻的人和 SDR 看得到。
- **場景**：對方回報你破音／鄰台抱怨干擾 → 降 MIC GAIN、嘴巴離麥遠一點。刻意小聲則沒有任何好處，只會讓對方收到的音量與訊噪比變差。

---

## Demo 1 ── 頻率＝水平位置
**機器選單：VFO 頻率　｜　只按 PTT，不用說話**

1. 無線電在 144.900，按 PTT → 記住那根線的橫軸位置。
2. 轉到下一頻道（如 145.000），按 PTT → **線往右跳**。

- **一句話**：頻率就是這根線的左右位置。
- **場景**：聽得到對方卻叫不到 → 先查頻率末位數字，比查天線還優先。
- 步進（STEP）獨立成 **Demo 4**，因為它牽涉的不只是「調不調得到」。

---

## Demo 2 ── 發射功率 H / M / L
**機器選單：TX POWER　｜　只按 PTT**

1. 同一頻率，功率設 **H**，按 PTT → 記住線的亮度（看右上訊號表數值）。
2. 改 **L**，再按 PTT → **線變暗、訊號表數值下降**。

- **一句話**：亮度＝相對強度；功率大只是線更亮，通不通還要看對方收不收得到。
- **場景**：夠用就好，滿功率只是更耗電、更燙、把干擾半徑無謂放大。
- ⚠️ 若太近，H 和 L 都頂到滿檔看不出差 → 把無線電再拿遠一點。

---

## Demo 3 ── 寬窄頻 W / N（其實在調頻偏）
**機器選單：W/N、WIDE/NARROW　｜　要持續說話**

1. 設 **寬頻 W**，用正常音量**持續講話 5~10 秒** → 看訊號**變寬**（往兩側長出調變邊帶）。
2. 改 **窄頻 N**，同樣講話 → 訊號**明顯變窄**。
3. 對照：寬頻約 16kHz、窄頻約 11kHz（滾輪放大、看 FFT 上訊號比噪音高處的寬度）。

- **一句話**：面板寫「頻寬」，其實是在改**頻偏**；一寬一窄，佔用頻寬就差一截。
- **場景**：跨單位一邊寬一邊窄 → 聲音一個破一個太小，兩邊都以為是對方訊號弱。統一 W/N 並寫進通聯計畫。

---

## Demo 4 ── 頻道步進與鄰頻重疊（接續 Demo 3）
**機器選單：STEP / 頻率步進　｜　需要兩台**

> 這個 demo 只有在做完 Demo 3 之後才講得通：**步進決定頻道間隔，W/N 決定訊號寬度，兩者一夾才決定會不會撞在一起。**

**先看步進本身**（30 秒帶過）

機器設 25 kHz 步進，試調 145.0625 → **調不到**；改 12.5 kHz → **調得到**。
步進決定你能停在哪些位置上。

**主戲：把兩台放在相鄰頻道，看訊號疊不疊**

1. A 設 **145.050**，B 設 **145.0625**（相隔 **12.5 kHz**）。
2. 兩台都設 **W**，輪流按 PTT 說話（或播測試音）→ **兩坨的邊帶明顯重疊**，中間沒有乾淨的間隙。
3. 兩台都改 **N**，重來 → **兩坨分開了**，中間看得到黑色。
4. B 改到 **145.075**（相隔 **25 kHz**），兩台都設回 **W** → 又分開了。

**保護頻帶＝頻道間隔 − 佔用頻寬**

| 設定 | 佔用頻寬 | 頻道間隔 | 保護頻帶 | |
|---|---|---|---|---|
| W | 16 kHz | 25 kHz | **+9 kHz** | ✅ 安全 |
| W | 16 kHz | **12.5 kHz** | **−3.5 kHz** | ❌ **必定重疊** |
| N | 11 kHz | 25 kHz | +14 kHz | ✅ 但浪費頻譜 |
| N | 11 kHz | 12.5 kHz | +1.5 kHz | ⚠️ 勉強夠 |

負數就是重疊。**這就是為什麼 25 kHz 間隔配寬頻、12.5 kHz 間隔配窄頻** —— 不是規定要你這樣配，是算出來只能這樣配。
（講義有互動式保護頻帶計算器，當場輸入數字讓它跳出負值最有效。）

- **一句話**：步進不只決定你調不調得到，還決定你**會不會壓到隔壁**。
- **最惡劣的情況**：在 12.5 kHz 頻道規劃上設成寬頻的人，**自己完全無感** —— 他聽到的一切正常，受害的是鄰頻的人。只有第三方的 SDR 抓得到。
- **場景**：鄰頻抱怨被干擾、或你的頻道整個變成別人的通話 → 先算一次保護頻帶，再懷疑硬體。

---

## Demo 5 ── 亞音 CTCSS / DCS（**重點：亞音不是加密**）
**機器選單：TONE / CTCSS / DCS、TX TONE / RX TONE　｜　需要兩台**

設 A 發射、B 接收。
| 步驟 | A（發射） | B（接收） | B 出聲? | SDR 收到? |
|---|---|---|---|---|
| ① | 無亞音 | 無亞音 | ✅ | ✅ |
| ② | 88.5Hz | 無亞音 | ✅ | ✅ |
| ③ | 88.5Hz | 88.5Hz | ✅ | ✅ |
| ④ | 88.5Hz | 100.0Hz | ❌ | ✅ |
| ⑤ | 無亞音 | 88.5Hz | ❌ | ✅ |

- **關鍵**：不管亞音對不對，**SDR 上的訊號長得一模一樣、照收不誤**。亞音只是決定「B 的喇叭開不開」。
- **一句話**：亞音（PL/Private Line）**不提供任何保密**，只是讓你的喇叭安靜。
- **場景**：中繼台叫不開 → 多半是 TX 亞音沒設或設錯；注意有些台 TX/RX 亞音不同。
- **通聯紀律**：人員位置、傷患個資、物資數量，**設了亞音也不能明語講**。

---

## Demo 6 ── 靜噪 SQL（併在 Demo 5 後講）
**機器選單：SQUELCH LEVEL**

- 在 SDR++ 把 Squelch Mode 開/關，或在無線電上調 SQL 門檻。
- **重點**：SQL 只關**你自己的喇叭**，對 RF 毫無影響 → 瀑布圖照收。
- **一句話**：雜訊靜噪、亞音、靜音——**三種「讓喇叭安靜」的機制，沒有一種影響發射的電波，也沒有一種能保密**。

---

## Demo 7 ── VOX 聲控發射
**機器選單：VOX / VOX GAIN / VOX DELAY**

1. 開 VOX，不按 PTT，直接講話 → 瀑布圖上載波**隨語音自動起停**。
2. 停止說話 → 過了 VOX DELAY 才收 → 看載波延遲一下才消失。

- **一句話**：聲音一大就自動發射；環境吵時容易誤觸發、關鍵時刻頭一個字被吃掉。
- **場景**：吵雜環境關 VOX 改手動 PTT；或調高 VOX GAIN 門檻、調 ANTI-VOX。

---

## Demo 8 ── 發射逾時 TOT
**機器選單：TOT / TIME-OUT TIMER**

1. 設一個短 TOT（如 30 秒），按住 PTT 不放 → 瀑布圖上載波持續一段後**自己中斷**。

- **一句話**：防止卡麥一直佔用頻道；時間到強制放開。
- **場景**：背包壓到 PTT 整條頻道被壓死 → TOT 自動解除。

---

## Demo 9 ── 訊號真偽 / 過載（壓軸，最有戲）
**需要：把 SDR 天線接回，或把無線電貼很近、增益開大**

1. 先乾淨狀態（天線拔掉、低增益）：按 PTT → **只有一根真載波**。
2. 製造過載：**接回天線 or 拉高 Gain 到 38** → 瀑布圖冒出**一整排等距假峰**。
3. 用 PTT 開關法：只有跟著 PTT 亮滅的是真的，其餘全是假。
4. 進階：把中心頻率移 100 kHz → **真訊號的絕對頻率不動，假訊號跟著中心跑**。

（鄰頻重疊已移到 Demo 4，這裡專講「憑空生出來的東西」。）

- **一句話**：收太多（增益太高／太近）會讓接收機**無中生有**假訊號；真訊號會跟 PTT 連動。
- **場景**：「聽到不該存在的東西」先懷疑自己過載 → 降增益、加衰減、換掉寬頻天線。

---

## 收尾：色階不是絕對功率（回扣開場）
- 同一根訊號，把色階 Max 拉高 → 看起來變窄；拉低 → 看起來變寬。
- **「看起來的寬度」不是佔用頻寬。** SDR 顯示的是 dBFS（相對），不是 dBm；未校正只能做相對比較。

---

### 道具建議
- 有**衰減器＋假負載**：功率、過載 demo 更漂亮可控。
- 想看**亞音本體**：SDR++ 錄一段，丟 Audacity 看音訊頻譜，能看到語音底下那條 67~254Hz 的亞音。
